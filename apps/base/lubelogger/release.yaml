apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: lubelogger
  namespace: lubelogger
spec:
  interval: 1h
  chart:
    spec:
      chart: lubelogger
      version: "1.4.7"
      sourceRef:
        kind: HelmRepository
        name: anza-labs
        namespace: flux-system
