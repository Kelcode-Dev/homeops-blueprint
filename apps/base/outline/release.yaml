apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: outline
  namespace: outline
spec:
  interval: 1h
  chart:
    spec:
      chart: outline
      version: "0.7.3"
      sourceRef:
        kind: HelmRepository
        name: community-charts
        namespace: flux-system
