apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cilium
  namespace: kube-system
spec:
  values:
    k8sServiceHost: 192.168.0.120  # CHANGE this to your VIP
    k8sServicePort: 6443
    prometheus:
      enabled: true
