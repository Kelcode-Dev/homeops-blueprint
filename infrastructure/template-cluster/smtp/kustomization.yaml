apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/smtp
  - smtp-secrets.sops.yaml

patches:
  - path: smtp-patch.yaml
    target:
      kind: HelmRelease
      name: smtp
