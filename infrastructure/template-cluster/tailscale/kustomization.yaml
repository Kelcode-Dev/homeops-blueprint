apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/tailscale
  - tailscale-secrets.sops.yaml

patches:
  - path: tailscale-patch.yaml
    target:
      kind: HelmRelease
      name: tsop
