apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  values:
    service:
      annotations:
        io.cilium/lb-ipam-ips: "192.168.0.230" # CHANGE THIS to match your vLAN
      externalTrafficPolicy: Local
